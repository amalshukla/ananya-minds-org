import { createFileRoute, Link } from "@tanstack/react-router";
import { ArrowRight, CheckCircle2 } from "lucide-react";
import { PublicLayout } from "@/components/site-shell";
import { Button } from "@/components/ui/button";
import { services } from "@/lib/content";

export const Route = createFileRoute("/services")({ head: () => ({ meta: [{ title: "Therapy & Assessments | Mindwell" }, { name: "description", content: "Explore personalized therapy, relationship support, and psychological assessment services." }, { property: "og:title", content: "Therapy & Assessments | Mindwell" }, { property: "og:description", content: "Professional support tailored to your needs." }], links: [{ rel: "canonical", href: "/services" }] }), component: ServicesPage });
function ServicesPage() { return <PublicLayout><section className="bg-secondary/55 px-5 py-20 text-center"><p className="text-sm font-bold uppercase tracking-[.18em] text-primary">Our services</p><h1 className="mx-auto mt-4 max-w-3xl text-5xl font-semibold tracking-tight">Care designed around your life, not a label.</h1><p className="mx-auto mt-6 max-w-2xl text-lg text-muted-foreground">Thoughtful, evidence-based support for individuals, couples, children, and families.</p></section><section className="mx-auto max-w-6xl px-5 py-20"><div className="grid gap-6 md:grid-cols-2">{services.map(({slug,title,description,icon:Icon})=><article key={slug} className="rounded-3xl border border-border bg-card p-8 shadow-[var(--shadow-soft)]"><Icon className="size-10 text-primary" /><h2 className="mt-6 text-2xl font-semibold">{title}</h2><p className="mt-3 leading-7 text-muted-foreground">{description}</p><ul className="mt-5 space-y-2 text-sm">{["Personalized care plan","Online and in-person options","Confidential, inclusive support"].map(x=><li key={x} className="flex gap-2"><CheckCircle2 className="size-5 text-primary" />{x}</li>)}</ul><Button variant="link" className="mt-5 px-0" asChild><Link to="/book">Book this service <ArrowRight /></Link></Button></article>)}</div></section></PublicLayout> }