import { createFileRoute } from "@tanstack/react-router";
import { Clock3, Mail, MapPin, Phone } from "lucide-react";
import { PublicLayout } from "@/components/site-shell";
import { Button } from "@/components/ui/button";
import { Input } from "@/components/ui/input";

export const Route = createFileRoute("/contact")({ head: () => ({ meta: [{ title: "Contact Mindwell Psychology" }, { name: "description", content: "Contact Mindwell for appointments, questions, and clinic information." }, { property: "og:title", content: "Contact Mindwell Psychology" }, { property: "og:description", content: "We are here to help you find the right next step." }], links: [{ rel: "canonical", href: "/contact" }] }), component: ContactPage });
function ContactPage(){return <PublicLayout><section className="mx-auto grid max-w-7xl gap-14 px-5 py-20 lg:grid-cols-[.85fr_1.15fr] lg:px-8"><div><p className="text-sm font-bold uppercase tracking-[.18em] text-primary">Contact</p><h1 className="mt-4 text-5xl font-semibold tracking-tight">Let us help you find the right next step.</h1><p className="mt-5 text-lg leading-8 text-muted-foreground">Send a message and our care coordinator will respond within one business day.</p><div className="mt-10 space-y-5 text-sm">{[[Phone,"(555) 014-2233"],[Mail,"hello@mindwell.example"],[MapPin,"120 Calm Street, Suite 4"],[Clock3,"Mon–Fri, 8:00–18:00"]].map(([Icon,text])=>{const C=Icon as typeof Phone;return <div key={text as string} className="flex items-center gap-3"><C className="size-5 text-primary" />{text as string}</div>})}</div></div><form className="rounded-3xl border border-border bg-card p-7 shadow-[var(--shadow-soft)]" onSubmit={(e)=>e.preventDefault()}><div className="grid gap-5 sm:grid-cols-2"><label className="text-sm font-medium">Name<Input className="mt-2 h-12" required maxLength={100} /></label><label className="text-sm font-medium">Email<Input className="mt-2 h-12" type="email" required maxLength={255} /></label></div><label className="mt-5 block text-sm font-medium">How can we help?<textarea className="mt-2 min-h-40 w-full rounded-xl border border-input bg-background p-4 outline-none focus-visible:ring-2 focus-visible:ring-ring" required maxLength={1000} /></label><p className="mt-3 text-xs text-muted-foreground">Please do not include urgent or highly sensitive medical information.</p><Button type="submit" className="mt-6 w-full">Send message</Button></form></section></PublicLayout>}