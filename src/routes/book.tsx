import { createFileRoute } from "@tanstack/react-router";
import { CalendarDays, Check, Clock, Laptop, MapPin } from "lucide-react";
import { useState } from "react";
import { PublicLayout } from "@/components/site-shell";
import { Button } from "@/components/ui/button";
import { services } from "@/lib/content";

export const Route = createFileRoute("/book")({ head: () => ({ meta: [{ title: "Book a Psychology Appointment | Mindwell" }, { name: "description", content: "Choose a service, appointment format, and time for your Mindwell consultation." }, { property: "og:title", content: "Book an Appointment | Mindwell" }, { property: "og:description", content: "A simple, private way to take the next step." }], links: [{ rel: "canonical", href: "/book" }] }), component: BookPage });
function BookPage(){const [step,setStep]=useState(1);const [service,setService]=useState(services[0].title);const [format,setFormat]=useState("Online");return <PublicLayout><section className="mx-auto max-w-5xl px-5 py-14"><div className="text-center"><p className="text-sm font-bold uppercase tracking-[.18em] text-primary">Private & secure booking</p><h1 className="mt-4 text-4xl font-semibold">Find a time that feels right</h1></div><ol className="mx-auto mt-10 flex max-w-xl items-center justify-between" aria-label="Booking progress">{["Support","Format","Time"].map((label,i)=><li key={label} className="flex flex-col items-center gap-2 text-xs text-muted-foreground"><span className={step>i?"grid size-9 place-items-center rounded-full bg-primary text-primary-foreground":"grid size-9 place-items-center rounded-full border border-border bg-card"}>{step>i+1?<Check/>:i+1}</span>{label}</li>)}</ol><div className="mt-10 rounded-3xl border border-border bg-card p-6 shadow-[var(--shadow-soft)] sm:p-10">{step===1&&<div><h2 className="text-2xl font-semibold">What kind of support are you looking for?</h2><div className="mt-6 grid gap-3 sm:grid-cols-2">{services.map(({title,description})=><button key={title} type="button" onClick={()=>setService(title)} className={service===title?"rounded-2xl border-2 border-primary bg-secondary p-5 text-left":"rounded-2xl border border-border p-5 text-left hover:border-primary/40"}><strong>{title}</strong><span className="mt-2 block text-sm text-muted-foreground">{description}</span></button>)}</div></div>}{step===2&&<div><h2 className="text-2xl font-semibold">How would you like to meet?</h2><div className="mt-6 grid gap-4 sm:grid-cols-2">{[["Online",Laptop,"Secure video from your own space"],["In person",MapPin,"Visit our calm, accessible clinic"]].map(([label,Icon,desc])=>{const C=Icon as typeof Laptop;return <button key={label as string} type="button" onClick={()=>setFormat(label as string)} className={format===label?"rounded-2xl border-2 border-primary bg-secondary p-6 text-left":"rounded-2xl border border-border p-6 text-left"}><C className="text-primary"/><strong className="mt-4 block">{label as string}</strong><span className="mt-2 block text-sm text-muted-foreground">{desc as string}</span></button>})}</div></div>}{step===3&&<div><h2 className="text-2xl font-semibold">Choose an available time</h2><div className="mt-6 flex items-center gap-3 rounded-xl bg-secondary p-4"><CalendarDays className="text-primary"/><span className="font-medium">Tuesday, June 16</span></div><div className="mt-4 grid grid-cols-2 gap-3 sm:grid-cols-4">{["9:00 AM","10:30 AM","1:00 PM","3:30 PM"].map(t=><button type="button" key={t} className="flex min-h-12 items-center justify-center gap-2 rounded-xl border border-border hover:border-primary hover:bg-secondary"><Clock className="size-4"/>{t}</button>)}</div><div className="mt-8 rounded-2xl border border-border bg-background p-5"><strong>Booking summary</strong><p className="mt-2 text-sm text-muted-foreground">{service} · {format} · 50 minutes</p></div></div>}<div className="mt-8 flex justify-between"><Button variant="ghost" disabled={step===1} onClick={()=>setStep(step-1)}>Back</Button><Button onClick={()=>step<3&&setStep(step+1)}>{step===3?"Continue to account":"Continue"}</Button></div></div></section></PublicLayout>}