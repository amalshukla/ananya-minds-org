import { createFileRoute, useNavigate } from "@tanstack/react-router";
import { Brain } from "lucide-react";
import { useState } from "react";
import { Button } from "@/components/ui/button";
import { Input } from "@/components/ui/input";
import { supabase } from "@/integrations/supabase/client";

export const Route = createFileRoute("/register")({ head: () => ({ meta: [{ title: "Create Account | Mindwell" }, { name: "description", content: "Create your secure Mindwell care account." }, { name: "robots", content: "noindex" }] }), component: RegisterPage });
function RegisterPage(){const [name,setName]=useState("");const [email,setEmail]=useState("");const [password,setPassword]=useState("");const [message,setMessage]=useState("");const [loading,setLoading]=useState(false);const navigate=useNavigate();async function register(){setLoading(true);setMessage("");const {error}=await supabase.auth.signUp({email,password,options:{emailRedirectTo:window.location.origin,data:{display_name:name}}});setLoading(false);if(error){setMessage(error.message);return;}setMessage("Check your email to verify your account.");}return <main className="flex min-h-dvh items-center justify-center bg-secondary/55 px-5 py-12"><div className="w-full max-w-md rounded-3xl border border-border bg-card p-8 shadow-[var(--shadow-soft)]"><button type="button" onClick={()=>navigate({to:"/"})} className="flex items-center gap-2 font-display text-lg"><Brain className="text-primary"/>Mindwell</button><h1 className="mt-9 text-3xl font-semibold">Create your care account</h1><p className="mt-2 text-sm text-muted-foreground">Private, secure, and designed around your wellbeing.</p><form className="mt-7 space-y-5" onSubmit={(e)=>{e.preventDefault();void register()}}><label className="block text-sm font-medium">Full name<Input className="mt-2 h-12" value={name} onChange={e=>setName(e.target.value)} required maxLength={120} autoComplete="name"/></label><label className="block text-sm font-medium">Email<Input className="mt-2 h-12" type="email" value={email} onChange={e=>setEmail(e.target.value)} required maxLength={255} autoComplete="email"/></label><label className="block text-sm font-medium">Password<Input className="mt-2 h-12" type="password" value={password} onChange={e=>setPassword(e.target.value)} required minLength={8} maxLength={128} autoComplete="new-password"/><span className="mt-2 block text-xs text-muted-foreground">Use at least 8 characters.</span></label>{message&&<p role="status" className="text-sm text-primary">{message}</p>}<Button className="w-full" type="submit" disabled={loading}>{loading?"Creating account…":"Create account"}</Button></form><p className="mt-6 text-center text-sm text-muted-foreground">Already have an account? <a className="font-semibold text-primary" href="/auth">Sign in</a></p></div></main>}